<UserControl x:Class="ja_training_szponcenie.Views.TrendsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1200"
             Background="{StaticResource BackgroundColor}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24">

            <!-- ==================== SEKCJA: HEADER ==================== -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Lewy górny róg - Back button i tytuł -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="←" Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent" FontSize="24" Padding="12,8" Margin="0,0,16,0"/>
                    <TextBlock Text="Trendy i korelacje" FontSize="28" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryColor}" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Centrum - Selektor zakresu czasowego -->
                <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ComboBox Width="250" Padding="12,8" FontSize="14" Margin="0,0,0,8">
                        <ComboBoxItem Content="Ostatnie 4 tygodnie" IsSelected="True"/>
                        <ComboBoxItem Content="Ostatnie 8 tygodni"/>
                        <ComboBoxItem Content="Ostatnie 3 miesiące"/>
                        <ComboBoxItem Content="Ostatnie 6 miesięcy"/>
                        <ComboBoxItem Content="Ostatni rok"/>
                        <ComboBoxItem Content="Niestandardowy..."/>
                    </ComboBox>
                    <TextBlock Text="1 września - 14 listopada 2025" FontSize="12"
                               Foreground="{StaticResource TextSecondaryColor}" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Prawy górny róg - Akcje -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="💾" Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent" Padding="12,8" Margin="0,0,8,0"
                            ToolTip="Zapisz analizę"/>
                    <Button Content="📤" Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent" Padding="12,8" Margin="0,0,8,0"
                            ToolTip="Eksportuj"/>
                    <Button Content="⋮" Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent" Padding="12,8"
                            ToolTip="Opcje"/>
                </StackPanel>
            </Grid>

            <!-- ==================== SEKCJA: WYBÓR METRYK ==================== -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Panel Metryki 1 (niebieski) -->
                <Border Grid.Column="0" CornerRadius="12" Padding="20" Margin="0,0,12,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#E3F2FD" Offset="0"/>
                            <GradientStop Color="#BBDEFB" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="12" ShadowDepth="4"/>
                    </Border.Effect>

                    <StackPanel>
                        <TextBlock Text="Wybierz pierwszą metrykę" FontSize="16" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryColor}" Margin="0,0,0,16"/>

                        <ComboBox Padding="12,10" FontSize="14" Margin="0,0,0,20">
                            <ComboBoxItem Content="── Trening ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="TSS dzienny" IsSelected="True"/>
                            <ComboBoxItem Content="Tygodniowy TSS"/>
                            <ComboBoxItem Content="Czas treningu (godz/tydzień)"/>
                            <ComboBoxItem Content="Średnia moc"/>
                            <ComboBoxItem Content="Normalized Power"/>
                            <ComboBoxItem Content="Intensity Factor"/>
                            <ComboBoxItem Content="Training Load (CTL)"/>
                            <ComboBoxItem Content="Fatigue (ATL)"/>
                            <ComboBoxItem Content="Form (TSB)"/>
                            <ComboBoxItem Content="Liczba treningów/tydzień"/>
                            <ComboBoxItem Content="Średni dystans"/>
                            <ComboBoxItem Content="Średnia prędkość/pace"/>

                            <ComboBoxItem Content="── Sen ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="Sleep Score"/>
                            <ComboBoxItem Content="Czas snu (godziny)"/>
                            <ComboBoxItem Content="Czas w Deep Sleep"/>
                            <ComboBoxItem Content="Czas w REM"/>
                            <ComboBoxItem Content="Liczba przebudzeń"/>
                            <ComboBoxItem Content="Czas zasypiania"/>
                            <ComboBoxItem Content="Regularność snu"/>
                            <ComboBoxItem Content="RHR podczas snu"/>

                            <ComboBoxItem Content="── Regeneracja ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="Poranne HRV"/>
                            <ComboBoxItem Content="Tętno spoczynkowe (RHR)"/>
                            <ComboBoxItem Content="Variability Score"/>
                            <ComboBoxItem Content="Recovery Score"/>

                            <ComboBoxItem Content="── Żywienie ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="Spożyte kalorie"/>
                            <ComboBoxItem Content="Bilans kaloryczny"/>
                            <ComboBoxItem Content="Białko (g)"/>
                            <ComboBoxItem Content="Węglowodany (g)"/>
                            <ComboBoxItem Content="Tłuszcze (g)"/>
                            <ComboBoxItem Content="% białka w diecie"/>
                            <ComboBoxItem Content="Regularność posiłków"/>

                            <ComboBoxItem Content="── Wellness ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="Wellness Score"/>
                            <ComboBoxItem Content="Poziom stresu"/>
                            <ComboBoxItem Content="SpO2"/>
                            <ComboBoxItem Content="Temperatura ciała"/>
                            <ComboBoxItem Content="Liczba kroków"/>
                            <ComboBoxItem Content="Waga"/>

                            <ComboBoxItem Content="── Inne ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="Dzień miesiąca"/>
                            <ComboBoxItem Content="Dzień tygodnia"/>
                            <ComboBoxItem Content="Temperatura na zewnątrz"/>
                            <ComboBoxItem Content="Wilgotność"/>
                        </ComboBox>

                        <!-- Podgląd wybranej metryki -->
                        <Border Background="White" CornerRadius="8" Padding="16" Opacity="0.95">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBlock Text="🏋️" FontSize="32" Margin="0,0,12,0"/>
                                    <StackPanel>
                                        <TextBlock Text="TSS dzienny" FontSize="18" FontWeight="Bold"/>
                                        <TextBlock Text="Średnio: 285 TSS/tydzień" FontSize="12"
                                                   Foreground="{StaticResource TextSecondaryColor}"/>
                                    </StackPanel>
                                </StackPanel>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Min: 180" FontSize="11"
                                               Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Grid.Column="1" Text="Max: 420" FontSize="11"
                                               Foreground="{StaticResource TextSecondaryColor}" HorizontalAlignment="Right"/>
                                </Grid>

                                <!-- Mini sparkline placeholder -->
                                <Border Height="40" Background="#F5F5F5" CornerRadius="4">
                                    <TextBlock Text="[mini sparkline]" FontSize="10" FontStyle="Italic"
                                               Foreground="{StaticResource TextSecondaryColor}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>

                <!-- Przycisk VS (centrum) -->
                <Border Grid.Column="1" Background="{StaticResource PrimaryColor}"
                        Width="56" Height="56" CornerRadius="28"
                        VerticalAlignment="Center">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="12" ShadowDepth="4"/>
                    </Border.Effect>
                    <Button Content="⇄" FontSize="24" FontWeight="Bold" Foreground="White"
                            Background="Transparent" BorderThickness="0"
                            ToolTip="Zamień metryki miejscami"/>
                </Border>

                <!-- Panel Metryki 2 (pomarańczowy) -->
                <Border Grid.Column="2" CornerRadius="12" Padding="20" Margin="12,0,0,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FFF3E0" Offset="0"/>
                            <GradientStop Color="#FFE0B2" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="12" ShadowDepth="4"/>
                    </Border.Effect>

                    <StackPanel>
                        <TextBlock Text="Wybierz drugą metrykę" FontSize="16" FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryColor}" Margin="0,0,0,16"/>

                        <ComboBox Padding="12,10" FontSize="14" Margin="0,0,0,20">
                            <ComboBoxItem Content="── Trening ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="TSS dzienny"/>
                            <ComboBoxItem Content="Tygodniowy TSS"/>
                            <ComboBoxItem Content="Czas treningu (godz/tydzień)"/>
                            <ComboBoxItem Content="Średnia moc"/>
                            <ComboBoxItem Content="Normalized Power"/>
                            <ComboBoxItem Content="Intensity Factor"/>
                            <ComboBoxItem Content="Training Load (CTL)"/>
                            <ComboBoxItem Content="Fatigue (ATL)"/>
                            <ComboBoxItem Content="Form (TSB)"/>
                            <ComboBoxItem Content="Liczba treningów/tydzień"/>
                            <ComboBoxItem Content="Średni dystans"/>
                            <ComboBoxItem Content="Średnia prędkość/pace"/>

                            <ComboBoxItem Content="── Sen ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="Sleep Score" IsSelected="True"/>
                            <ComboBoxItem Content="Czas snu (godziny)"/>
                            <ComboBoxItem Content="Czas w Deep Sleep"/>
                            <ComboBoxItem Content="Czas w REM"/>
                            <ComboBoxItem Content="Liczba przebudzeń"/>
                            <ComboBoxItem Content="Czas zasypiania"/>
                            <ComboBoxItem Content="Regularność snu"/>
                            <ComboBoxItem Content="RHR podczas snu"/>

                            <ComboBoxItem Content="── Regeneracja ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="Poranne HRV"/>
                            <ComboBoxItem Content="Tętno spoczynkowe (RHR)"/>
                            <ComboBoxItem Content="Variability Score"/>
                            <ComboBoxItem Content="Recovery Score"/>

                            <ComboBoxItem Content="── Żywienie ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="Spożyte kalorie"/>
                            <ComboBoxItem Content="Bilans kaloryczny"/>
                            <ComboBoxItem Content="Białko (g)"/>
                            <ComboBoxItem Content="Węglowodany (g)"/>
                            <ComboBoxItem Content="Tłuszcze (g)"/>
                            <ComboBoxItem Content="% białka w diecie"/>
                            <ComboBoxItem Content="Regularność posiłków"/>

                            <ComboBoxItem Content="── Wellness ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="Wellness Score"/>
                            <ComboBoxItem Content="Poziom stresu"/>
                            <ComboBoxItem Content="SpO2"/>
                            <ComboBoxItem Content="Temperatura ciała"/>
                            <ComboBoxItem Content="Liczba kroków"/>
                            <ComboBoxItem Content="Waga"/>

                            <ComboBoxItem Content="── Inne ──" IsEnabled="False" FontWeight="Bold"/>
                            <ComboBoxItem Content="Dzień miesiąca"/>
                            <ComboBoxItem Content="Dzień tygodnia"/>
                            <ComboBoxItem Content="Temperatura na zewnątrz"/>
                            <ComboBoxItem Content="Wilgotność"/>
                        </ComboBox>

                        <!-- Podgląd wybranej metryki -->
                        <Border Background="White" CornerRadius="8" Padding="16" Opacity="0.95">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                    <TextBlock Text="😴" FontSize="32" Margin="0,0,12,0"/>
                                    <StackPanel>
                                        <TextBlock Text="Sleep Score" FontSize="18" FontWeight="Bold"/>
                                        <TextBlock Text="Średnio: 78/100" FontSize="12"
                                                   Foreground="{StaticResource TextSecondaryColor}"/>
                                    </StackPanel>
                                </StackPanel>

                                <Grid Margin="0,0,0,8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="Min: 52" FontSize="11"
                                               Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Grid.Column="1" Text="Max: 94" FontSize="11"
                                               Foreground="{StaticResource TextSecondaryColor}" HorizontalAlignment="Right"/>
                                </Grid>

                                <!-- Mini sparkline placeholder -->
                                <Border Height="40" Background="#F5F5F5" CornerRadius="4">
                                    <TextBlock Text="[mini sparkline]" FontSize="10" FontStyle="Italic"
                                               Foreground="{StaticResource TextSecondaryColor}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- ==================== SEKCJA: WYKRES KORELACJI ==================== -->
            <StackPanel Margin="0,0,0,24">
                <Grid Margin="0,0,0,16">
                    <TextBlock Text="📊 Wykres korelacji" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <CheckBox Content="Pokaż linię trendu" IsChecked="True" Margin="0,0,16,0"/>
                        <CheckBox Content="Pokaż pas ufności" IsChecked="True" Margin="0,0,16,0"/>
                        <Button Content="↺" Style="{StaticResource PrimaryButtonStyle}"
                                Background="Transparent" Padding="8" ToolTip="Resetuj widok"/>
                        <Button Content="📷" Style="{StaticResource PrimaryButtonStyle}"
                                Background="Transparent" Padding="8" Margin="8,0,0,0" ToolTip="Eksportuj do PNG"/>
                    </StackPanel>
                </Grid>

                <!-- Placeholder dla wykresu scatter plot -->
                <Border BorderBrush="{StaticResource DividerColor}" BorderThickness="1" CornerRadius="8"
                        Height="400" Background="White" Padding="16">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8" ShadowDepth="2"/>
                    </Border.Effect>

                    <Grid>
                        <TextBlock Text="[WYKRES SCATTER PLOT - KORELACJA]" FontSize="24" FontStyle="Italic"
                                   Foreground="{StaticResource TextSecondaryColor}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <!-- Etykiety osi (przykładowe) -->
                        <TextBlock Text="TSS dzienny →" FontSize="12" FontWeight="SemiBold"
                                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,8"/>
                        <TextBlock Text="Sleep Score ↑" FontSize="12" FontWeight="SemiBold"
                                   VerticalAlignment="Center" HorizontalAlignment="Left" Margin="8,0,0,0">
                            <TextBlock.RenderTransform>
                                <RotateTransform Angle="-90"/>
                            </TextBlock.RenderTransform>
                        </TextBlock>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- ==================== SEKCJA: STATYSTYKI KORELACJI ==================== -->
            <StackPanel Margin="0,0,0,24">
                <TextBlock Text="📈 Statystyki korelacji" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Kolumna 1: Współczynnik korelacji (R) -->
                    <Border Grid.Column="0" BorderBrush="{StaticResource DividerColor}" BorderThickness="1"
                            CornerRadius="8" Background="White" Padding="24" Margin="0,0,8,0">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8" ShadowDepth="2"/>
                        </Border.Effect>

                        <StackPanel HorizontalAlignment="Center">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                                <TextBlock Text="Współczynnik korelacji (R)" FontSize="14" FontWeight="SemiBold"/>
                                <TextBlock Text=" ⓘ" FontSize="12" Foreground="{StaticResource PrimaryColor}"
                                           ToolTip="Wartość od -1 do 1. Bliżej 1 = silniejsza dodatnia zależność"/>
                            </StackPanel>

                            <TextBlock Text="0.68" FontSize="48" FontWeight="Bold"
                                       Foreground="#2E7D32" HorizontalAlignment="Center"/>

                            <Border Background="#E8F5E9" CornerRadius="6" Padding="12,6" Margin="0,12,0,0">
                                <TextBlock Text="Silna dodatnia korelacja" FontSize="12"
                                           Foreground="#2E7D32" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Kolumna 2: R² (R-kwadrat) -->
                    <Border Grid.Column="1" BorderBrush="{StaticResource DividerColor}" BorderThickness="1"
                            CornerRadius="8" Background="White" Padding="24" Margin="8,0">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8" ShadowDepth="2"/>
                        </Border.Effect>

                        <StackPanel HorizontalAlignment="Center">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                                <TextBlock Text="R² (R-kwadrat)" FontSize="14" FontWeight="SemiBold"/>
                                <TextBlock Text=" ⓘ" FontSize="12" Foreground="{StaticResource PrimaryColor}"
                                           ToolTip="Pokazuje, jak dużą część zmienności jednej metryki można przewidzieć na podstawie drugiej"/>
                            </StackPanel>

                            <TextBlock Text="46%" FontSize="48" FontWeight="Bold"
                                       Foreground="{StaticResource PrimaryColor}" HorizontalAlignment="Center"/>

                            <TextBlock Text="Procent zmienności wyjaśnionej" FontSize="11"
                                       Foreground="{StaticResource TextSecondaryColor}"
                                       HorizontalAlignment="Center" Margin="0,12,0,12"/>

                            <!-- Progress bar -->
                            <Border Background="#E3F2FD" Height="24" CornerRadius="12" Width="200">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="46*"/>
                                        <ColumnDefinition Width="54*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="{StaticResource PrimaryColor}" CornerRadius="12,0,0,12"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>

                    <!-- Kolumna 3: P-value -->
                    <Border Grid.Column="2" BorderBrush="{StaticResource DividerColor}" BorderThickness="1"
                            CornerRadius="8" Background="White" Padding="24" Margin="8,0,0,0">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8" ShadowDepth="2"/>
                        </Border.Effect>

                        <StackPanel HorizontalAlignment="Center">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,12">
                                <TextBlock Text="P-value" FontSize="14" FontWeight="SemiBold"/>
                                <TextBlock Text=" ⓘ" FontSize="12" Foreground="{StaticResource PrimaryColor}"
                                           ToolTip="P-value pokazuje, czy korelacja jest przypadkowa. Wartość &lt; 0.05 = mało prawdopodobne, że to przypadek"/>
                            </StackPanel>

                            <TextBlock Text="p = 0.003" FontSize="36" FontWeight="Bold"
                                       Foreground="{StaticResource SuccessColor}" HorizontalAlignment="Center"/>

                            <Border Background="#E8F5E9" CornerRadius="6" Padding="12,6" Margin="0,12,0,0">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="✓" FontSize="16" Foreground="{StaticResource SuccessColor}"
                                               FontWeight="Bold" Margin="0,0,8,0"/>
                                    <TextBlock Text="Statystycznie istotne" FontSize="12"
                                               Foreground="{StaticResource SuccessColor}" FontWeight="SemiBold"/>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Rozwijana sekcja - Więcej statystyk -->
                <Expander Header="Pokaż więcej statystyk ▼" Margin="0,16,0,0"
                          BorderBrush="{StaticResource DividerColor}" BorderThickness="1"
                          CornerRadius="8" Background="White" Padding="16">
                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,16,0">
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Liczba punktów danych:" FontSize="12"
                                           Foreground="{StaticResource TextSecondaryColor}"/>
                                <TextBlock Text="n = 56 dni" FontSize="12" FontWeight="SemiBold"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Średnia TSS dzienny:" FontSize="12"
                                           Foreground="{StaticResource TextSecondaryColor}"/>
                                <TextBlock Text="245" FontSize="12" FontWeight="SemiBold"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Odchylenie std. (TSS):" FontSize="12"
                                           Foreground="{StaticResource TextSecondaryColor}"/>
                                <TextBlock Text="±62" FontSize="12" FontWeight="SemiBold"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>

                        <StackPanel Grid.Column="1">
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Równanie linii trendu:" FontSize="12"
                                           Foreground="{StaticResource TextSecondaryColor}"/>
                                <TextBlock Text="y = 0.15x + 42" FontSize="12" FontWeight="SemiBold"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Średni Sleep Score:" FontSize="12"
                                           Foreground="{StaticResource TextSecondaryColor}"/>
                                <TextBlock Text="78" FontSize="12" FontWeight="SemiBold"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Margin="0,0,0,8">
                                <TextBlock Text="Odchylenie std. (Sleep):" FontSize="12"
                                           Foreground="{StaticResource TextSecondaryColor}"/>
                                <TextBlock Text="±11" FontSize="12" FontWeight="SemiBold"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Expander>
            </StackPanel>

            <!-- ==================== SEKCJA: WYKRES TRENDÓW W CZASIE ==================== -->
            <StackPanel Margin="0,0,0,24">
                <Grid Margin="0,0,0,16">
                    <TextBlock Text="📉 Trendy w czasie" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <RadioButton Content="Nałożone wykresy" IsChecked="True"
                                     GroupName="ChartType" Margin="0,0,16,0"/>
                        <RadioButton Content="Osobne wykresy" GroupName="ChartType"/>
                    </StackPanel>
                </Grid>

                <!-- Wariant 1: Nałożone wykresy (domyślny) -->
                <Border BorderBrush="{StaticResource DividerColor}" BorderThickness="1" CornerRadius="8"
                        Height="300" Background="White" Padding="16">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8" ShadowDepth="2"/>
                    </Border.Effect>

                    <Grid>
                        <TextBlock Text="[WYKRES LINIOWY - NAKŁADAJĄCE SIĘ METRYKI W CZASIE]"
                                   FontSize="24" FontStyle="Italic"
                                   Foreground="{StaticResource TextSecondaryColor}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <!-- Legenda -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"
                                    VerticalAlignment="Top" Margin="16">
                            <CheckBox IsChecked="True" Margin="0,0,16,0">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="16" Height="16" Background="#2196F3" CornerRadius="2" Margin="0,0,8,0"/>
                                    <TextBlock Text="TSS dzienny" FontSize="11"/>
                                </StackPanel>
                            </CheckBox>
                            <CheckBox IsChecked="True">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="16" Height="16" Background="#FF9800" CornerRadius="2" Margin="0,0,8,0"/>
                                    <TextBlock Text="Sleep Score" FontSize="11"/>
                                </StackPanel>
                            </CheckBox>
                        </StackPanel>

                        <!-- Etykiety osi -->
                        <TextBlock Text="Czas →" FontSize="11" FontWeight="SemiBold"
                                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,0,0,8"/>
                    </Grid>
                </Border>
            </StackPanel>

            <!-- ==================== SEKCJA: INSIGHTS ==================== -->
            <StackPanel Margin="0,0,0,24">
                <TextBlock Text="💡 Automatyczne insights" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16"/>

                <Border CornerRadius="12" Padding="24">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#FFF9C4" Offset="0"/>
                            <GradientStop Color="#FFF59D" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="12" ShadowDepth="4"/>
                    </Border.Effect>

                    <StackPanel>
                        <!-- Insight 1 -->
                        <Border Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,12" Opacity="0.95">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="✓" FontSize="20" Foreground="{StaticResource SuccessColor}"
                                               FontWeight="Bold" Margin="0,0,12,0"/>
                                    <TextBlock Text="Silna pozytywna korelacja wykryta" FontSize="16" FontWeight="SemiBold"/>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextSecondaryColor}">
                                    Wyższe wartości TSS dziennego są silnie skorelowane z lepszym Sleep Score (R=0.68).
                                    To sugeruje, że większe obciążenie treningowe może prowadzić do głębszego i bardziej
                                    regenerującego snu.
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <!-- Insight 2 -->
                        <Border Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,12" Opacity="0.95">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="📊" FontSize="20" Margin="0,0,12,0"/>
                                    <TextBlock Text="Trend wzrostowy w ostatnich tygodniach" FontSize="16" FontWeight="SemiBold"/>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextSecondaryColor}">
                                    Ostatnie 2 tygodnie pokazują wzmocnienie korelacji (R wzrosło z 0.62 do 0.68).
                                    Twoje ciało może lepiej adaptować się do obciążenia treningowego.
                                </TextBlock>
                            </StackPanel>
                        </Border>

                        <!-- Insight 3 -->
                        <Border Background="White" CornerRadius="8" Padding="16" Opacity="0.95">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="💡" FontSize="20" Margin="0,0,12,0"/>
                                    <TextBlock Text="Rekomendacja" FontSize="16" FontWeight="SemiBold"/>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextSecondaryColor}">
                                    Rozważ zbadanie korelacji między TSS a innymi metrykami regeneracji (np. HRV, RHR)
                                    aby lepiej zrozumieć wpływ treningu na ogólną regenerację.
                                </TextBlock>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Border>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
