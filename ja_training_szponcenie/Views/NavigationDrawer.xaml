<UserControl x:Class="ja_training_szponcenie.Views.NavigationDrawer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="900" d:DesignWidth="320"
             Background="{StaticResource SurfaceColor}">

    <Border BorderBrush="{StaticResource DividerColor}" BorderThickness="0,0,1,0">
        <Border.Effect>
            <DropShadowEffect Color="#000000" Opacity="0.2" BlurRadius="16" ShadowDepth="0"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header -->
                <RowDefinition Height="*"/>    <!-- Menu Items -->
                <RowDefinition Height="Auto"/> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header menu -->
            <Border Grid.Row="0" Background="{StaticResource PrimaryColor}" Padding="20,24">
                <StackPanel>
                    <TextBlock Text="JA Training" FontSize="24" FontWeight="Bold" Foreground="White"/>
                    <StackPanel Orientation="Horizontal" Margin="0,12,0,0">
                        <Border Width="48" Height="48" CornerRadius="24" Background="White" Margin="0,0,12,0">
                            <TextBlock Text="👤" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Jan Kowalski" FontWeight="SemiBold" FontSize="14" Foreground="White"/>
                            <TextBlock Text="jan.kowalski@example.com" FontSize="11" Foreground="#E3F2FD"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Menu Items -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="0,8,0,8">
                    <!-- Sekcja Główna -->
                    <TextBlock Text="GŁÓWNA" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryColor}"
                               Margin="20,12,20,8"/>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="#E3F2FD"
                            Foreground="{StaticResource PrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏠" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Dashboard" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📅" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Kalendarz" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="➕" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Dodaj trening" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <!-- Separator -->
                    <Border Style="{StaticResource SeparatorStyle}" Margin="20,12"/>

                    <!-- Sekcja Analiza -->
                    <TextBlock Text="ANALIZA" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryColor}"
                               Margin="20,12,20,8"/>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📋" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Lista treningów" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📈" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Analiza treningu" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📉" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Power Curve" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🏆" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Rekordy osobiste" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <!-- Separator -->
                    <Border Style="{StaticResource SeparatorStyle}" Margin="20,12"/>

                    <!-- Sekcja Zdrowie i regeneracja -->
                    <TextBlock Text="ZDROWIE I REGENERACJA" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryColor}"
                               Margin="20,12,20,8"/>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="😴" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Sen" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🍽️" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Żywienie" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="❤️" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Tętno" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💪" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Wellness" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <!-- Separator -->
                    <Border Style="{StaticResource SeparatorStyle}" Margin="20,12"/>

                    <!-- Sekcja Inne -->
                    <TextBlock Text="INNE" FontSize="11" FontWeight="SemiBold"
                               Foreground="{StaticResource TextSecondaryColor}"
                               Margin="20,12,20,8"/>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="⚙️" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Ustawienia" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent"
                            Foreground="{StaticResource TextPrimaryColor}"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Padding="20,12"
                            Margin="8,2">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="ℹ️" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Text="Pomoc" FontSize="14" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" BorderBrush="{StaticResource DividerColor}" BorderThickness="0,1,0,0" Padding="20,16">
                <StackPanel>
                    <TextBlock Text="JA Training v1.0.0" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                    <TextBlock Text="© 2025 JA Training Team" FontSize="10" Foreground="{StaticResource TextSecondaryColor}" Margin="0,4,0,0"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
