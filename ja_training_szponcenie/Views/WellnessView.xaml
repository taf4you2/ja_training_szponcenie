<UserControl x:Class="ja_training_szponcenie.Views.WellnessView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="1000" d:DesignWidth="1200"
             Background="{StaticResource BackgroundColor}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="24">

            <!-- ==================== SEKCJA: HEADER ==================== -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Lewy górny róg - Back button i tytuł -->
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="←" Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent" FontSize="24" Padding="12,8" Margin="0,0,16,0"/>
                    <TextBlock Text="Wellness Dashboard" FontSize="28" FontWeight="Bold"
                               Foreground="{StaticResource TextPrimaryColor}" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Centrum - Selektor daty -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Button Content="◀" Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent" FontSize="20" Padding="12,8" Margin="0,0,16,0"/>
                    <TextBlock Text="Czwartek, 14 listopada 2025" FontSize="18" FontWeight="SemiBold"
                               VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button Content="▶" Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent" FontSize="20" Padding="12,8" Margin="0,0,16,0"/>
                    <Button Content="Dzisiaj" Style="{StaticResource PrimaryButtonStyle}"
                            Padding="12,8" Margin="0,0,0,0"/>
                </StackPanel>

                <!-- Prawy górny róg - Akcje -->
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="📊" Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent" Padding="12,8" Margin="0,0,8,0"
                            ToolTip="Historia"/>
                    <Button Content="⚙️" Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent" Padding="12,8" Margin="0,0,8,0"
                            ToolTip="Ustawienia"/>
                    <Button Content="⋮" Style="{StaticResource PrimaryButtonStyle}"
                            Background="Transparent" Padding="12,8"
                            ToolTip="Opcje"/>
                </StackPanel>
            </Grid>

            <!-- ==================== GŁÓWNA KARTA: WELLNESS SCORE ==================== -->
            <Border CornerRadius="16" Padding="32" Margin="0,0,0,24" Height="300">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#E8F5E9" Offset="0"/>
                        <GradientStop Color="#C8E6C9" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Border.Effect>
                    <DropShadowEffect Color="#000000" Opacity="0.15" BlurRadius="20" ShadowDepth="8"/>
                </Border.Effect>

                <Grid>
                    <!-- Lewy górny róg - Trend -->
                    <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                            <TextBlock Text="↗️" FontSize="20" Margin="0,0,8,0"/>
                            <TextBlock Text="+3 punkty" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource SuccessColor}"/>
                        </StackPanel>
                        <TextBlock Text="vs wczoraj" FontSize="12" Foreground="{StaticResource TextSecondaryColor}"/>
                    </StackPanel>

                    <!-- Prawy górny róg - Średnia 7 dni -->
                    <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top">
                        <TextBlock Text="Śr. 7 dni: 73" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <!-- Mini sparkline placeholder -->
                        <Border Width="100" Height="30" Background="White" CornerRadius="4" Opacity="0.5">
                            <TextBlock Text="📈" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </StackPanel>

                    <!-- Centralny pierścień wellness score -->
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                        <!-- Pierścień (placeholder - będzie implementowany jako custom control) -->
                        <Border Width="200" Height="200" CornerRadius="100" Margin="0,0,0,16">
                            <Border.Background>
                                <RadialGradientBrush>
                                    <GradientStop Color="White" Offset="0"/>
                                    <GradientStop Color="White" Offset="0.7"/>
                                    <GradientStop Color="#81C784" Offset="0.7"/>
                                    <GradientStop Color="#66BB6A" Offset="1"/>
                                </RadialGradientBrush>
                            </Border.Background>

                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <TextBlock Text="76" FontSize="72" FontWeight="Bold" Foreground="#2E7D32"/>
                                    <TextBlock Text="/100" FontSize="24" Foreground="#757575" VerticalAlignment="Bottom" Margin="4,0,0,12"/>
                                </StackPanel>
                                <TextBlock Text="Dobra gotowość ✓" FontSize="20" FontWeight="SemiBold"
                                           Foreground="#2E7D32" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                            </StackPanel>
                        </Border>

                        <!-- Rekomendacja treningowa -->
                        <Border Background="White" CornerRadius="12" Padding="16" Opacity="0.95" MaxWidth="500">
                            <Border.Effect>
                                <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="8" ShadowDepth="2"/>
                            </Border.Effect>
                            <StackPanel>
                                <TextBlock TextWrapping="Wrap" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8">
                                    <Run Text="✓"/>
                                    <Run Text="Możesz trenować normalnie. Unikaj przekraczania planned TSS o więcej niż 10%."/>
                                </TextBlock>
                                <Button Content="Zobacz szczegóły" Style="{StaticResource PrimaryButtonStyle}"
                                        Background="Transparent" Padding="8,4" HorizontalAlignment="Left"
                                        FontSize="12" BorderBrush="{StaticResource PrimaryColor}" BorderThickness="1"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>

                    <!-- Dolna część - Timestamp -->
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Bottom" Orientation="Horizontal">
                        <TextBlock Text="Zaktualizowano dzisiaj o 07:23" FontSize="11"
                                   Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,12,0"/>
                        <Button Content="↻" Style="{StaticResource PrimaryButtonStyle}"
                                Background="Transparent" Padding="4" FontSize="12"
                                ToolTip="Odśwież dane"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ==================== SEKCJA: BREAKDOWN KOMPONENTÓW ==================== -->
            <StackPanel Margin="0,0,0,24">
                <Grid Margin="0,0,0,16">
                    <TextBlock Text="Składowe wellness score" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    <TextBlock Text="ⓘ" FontSize="16" Foreground="{StaticResource PrimaryColor}"
                               HorizontalAlignment="Right" VerticalAlignment="Center"
                               ToolTip="Wellness score obliczany jest jako ważona średnia poszczególnych komponentów"/>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- KARTA 1: HRV -->
                    <Border Grid.Row="0" Grid.Column="0" BorderBrush="{StaticResource SuccessColor}" BorderThickness="2"
                            CornerRadius="12" Background="White" Padding="20" Margin="0,0,8,16">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="10" ShadowDepth="3"/>
                        </Border.Effect>

                        <StackPanel>
                            <!-- Nagłówek -->
                            <Grid Margin="0,0,0,16">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="❤️" FontSize="24" Margin="0,0,12,0"/>
                                    <TextBlock Text="HRV poranne" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="Waga: 25%" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Główna wartość -->
                            <StackPanel Margin="0,0,0,16">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="68 ms" FontSize="36" FontWeight="Bold"/>
                                    <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="↗️" FontSize="14" Margin="0,0,4,0"/>
                                            <TextBlock Text="+5 vs wczoraj" FontSize="12" Foreground="{StaticResource SuccessColor}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Status bar -->
                                <Border Background="#FFEBEE" Height="24" CornerRadius="12" Margin="0,0,0,8">
                                    <Grid>
                                        <Border CornerRadius="12">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                    <GradientStop Color="#EF5350" Offset="0"/>
                                                    <GradientStop Color="#FFA726" Offset="0.33"/>
                                                    <GradientStop Color="#FDD835" Offset="0.5"/>
                                                    <GradientStop Color="#9CCC65" Offset="0.67"/>
                                                    <GradientStop Color="#66BB6A" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>
                                        <!-- Marker aktualnej wartości -->
                                        <Border Width="12" Height="12" CornerRadius="6" Background="White"
                                                HorizontalAlignment="Left" Margin="140,0,0,0"
                                                BorderBrush="#2E7D32" BorderThickness="2"/>
                                    </Grid>
                                </Border>

                                <TextBlock Text="Powyżej normy ✓" FontSize="13" FontWeight="SemiBold"
                                           Foreground="{StaticResource SuccessColor}"/>
                            </StackPanel>

                            <!-- Dodatkowe info -->
                            <StackPanel>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Twój zakres normalny:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Text="55-75 ms" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="7-dniowa średnia:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Text="65 ms" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                            </StackPanel>

                            <Button Content="Szczegóły" Style="{StaticResource PrimaryButtonStyle}"
                                    Margin="0,12,0,0" Padding="8" FontSize="12" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>

                    <!-- KARTA 2: SLEEP SCORE -->
                    <Border Grid.Row="0" Grid.Column="1" BorderBrush="{StaticResource SuccessColor}" BorderThickness="2"
                            CornerRadius="12" Background="White" Padding="20" Margin="8,0,0,16">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="10" ShadowDepth="3"/>
                        </Border.Effect>

                        <StackPanel>
                            <!-- Nagłówek -->
                            <Grid Margin="0,0,0,16">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="😴" FontSize="24" Margin="0,0,12,0"/>
                                    <TextBlock Text="Sleep Score" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="Waga: 25%" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Główna wartość z mini pierścieniem -->
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- Mini pierścień -->
                                <Border Grid.Column="0" Width="80" Height="80" CornerRadius="40" Margin="0,0,16,0">
                                    <Border.Background>
                                        <RadialGradientBrush>
                                            <GradientStop Color="White" Offset="0"/>
                                            <GradientStop Color="White" Offset="0.6"/>
                                            <GradientStop Color="#66BB6A" Offset="0.6"/>
                                            <GradientStop Color="#4CAF50" Offset="1"/>
                                        </RadialGradientBrush>
                                    </Border.Background>
                                    <TextBlock Text="82" FontSize="32" FontWeight="Bold"
                                               Foreground="{StaticResource SuccessColor}"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>

                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                    <TextBlock Text="82/100" FontSize="28" FontWeight="Bold" Margin="0,0,0,8"/>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="→" FontSize="14" Margin="0,0,4,0"/>
                                        <TextBlock Text="bez zmian" FontSize="12" Foreground="{StaticResource TextSecondaryColor}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>

                            <!-- Breakdown snu -->
                            <StackPanel>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Czas snu:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="7h 32m" FontSize="11" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                        <TextBlock Text="✓" FontSize="11" Foreground="{StaticResource SuccessColor}"/>
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Deep Sleep:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="1h 18m (18%)" FontSize="11" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                        <TextBlock Text="✓" FontSize="11" Foreground="{StaticResource SuccessColor}"/>
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="REM Sleep:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="2h 05m (28%)" FontSize="11" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                        <TextBlock Text="✓" FontSize="11" Foreground="{StaticResource SuccessColor}"/>
                                    </StackPanel>
                                </Grid>
                                <Grid>
                                    <TextBlock Text="Przebudzenia:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="3 razy" FontSize="11" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                        <TextBlock Text="✓" FontSize="11" Foreground="{StaticResource SuccessColor}"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>

                            <Border Background="#E8F5E9" CornerRadius="6" Padding="8" Margin="0,12,0,12">
                                <TextBlock Text="Bardzo dobry sen, dobrze się zregenerowałeś" FontSize="11"
                                           TextWrapping="Wrap" Foreground="#2E7D32"/>
                            </Border>

                            <Button Content="Zobacz sen" Style="{StaticResource PrimaryButtonStyle}"
                                    Padding="8" FontSize="12" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>

                    <!-- KARTA 3: TRAINING LOAD BALANCE -->
                    <Border Grid.Row="1" Grid.Column="0" BorderBrush="{StaticResource SuccessColor}" BorderThickness="2"
                            CornerRadius="12" Background="White" Padding="20" Margin="0,0,8,16">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="10" ShadowDepth="3"/>
                        </Border.Effect>

                        <StackPanel>
                            <!-- Nagłówek -->
                            <Grid Margin="0,0,0,16">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🏋️" FontSize="24" Margin="0,0,12,0"/>
                                    <TextBlock Text="Training Load Balance" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="Waga: 20%" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Główna wartość -->
                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="+14" FontSize="36" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"/>
                                <TextBlock Text="Świeży" FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource SuccessColor}"/>
                            </StackPanel>

                            <!-- Mini PMC chart placeholder -->
                            <Border BorderBrush="{StaticResource DividerColor}" BorderThickness="1"
                                    CornerRadius="6" Height="60" Background="#F5F5F5" Margin="0,0,0,16">
                                <TextBlock Text="[MINI PMC CHART - CTL/ATL/TSB]" FontSize="10" FontStyle="Italic"
                                           Foreground="{StaticResource TextSecondaryColor}"
                                           HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>

                            <!-- Metryki PMC -->
                            <StackPanel>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Fitness (CTL):" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="62" FontSize="11" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                        <TextBlock Text="↗️ +1.2" FontSize="10" Foreground="{StaticResource SuccessColor}"/>
                                    </StackPanel>
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Fatigue (ATL):" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <TextBlock Text="48" FontSize="11" FontWeight="SemiBold" Margin="0,0,4,0"/>
                                        <TextBlock Text="↗️ +3.1" FontSize="10" Foreground="{StaticResource WarningColor}"/>
                                    </StackPanel>
                                </Grid>
                                <Grid>
                                    <TextBlock Text="Akut:Chronic Ratio:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Text="0.77" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                            </StackPanel>

                            <Border Background="#E8F5E9" CornerRadius="6" Padding="8" Margin="0,12,0,12">
                                <TextBlock Text="Dobry balans, jesteś świeży i gotowy" FontSize="11"
                                           TextWrapping="Wrap" Foreground="#2E7D32"/>
                            </Border>

                            <Button Content="Zobacz PMC" Style="{StaticResource PrimaryButtonStyle}"
                                    Padding="8" FontSize="12" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>

                    <!-- KARTA 4: RESTING HEART RATE -->
                    <Border Grid.Row="1" Grid.Column="1" BorderBrush="{StaticResource SuccessColor}" BorderThickness="2"
                            CornerRadius="12" Background="White" Padding="20" Margin="8,0,0,16">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="10" ShadowDepth="3"/>
                        </Border.Effect>

                        <StackPanel>
                            <!-- Nagłówek -->
                            <Grid Margin="0,0,0,16">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💓" FontSize="24" Margin="0,0,12,0"/>
                                    <TextBlock Text="Resting Heart Rate" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="Waga: 15%" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Główna wartość -->
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Margin="0,0,16,0">
                                    <TextBlock Text="52 bpm" FontSize="36" FontWeight="Bold"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="↘️" FontSize="14" Margin="0,0,4,0"/>
                                        <TextBlock Text="-1 bpm" FontSize="12" Foreground="{StaticResource SuccessColor}"/>
                                    </StackPanel>
                                </StackPanel>

                                <!-- Termometr wizualny -->
                                <Border Grid.Column="1" Width="30" Height="120" CornerRadius="15"
                                        Background="#FFEBEE" VerticalAlignment="Center">
                                    <Grid>
                                        <Border CornerRadius="15">
                                            <Border.Background>
                                                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                                                    <GradientStop Color="#EF5350" Offset="0"/>
                                                    <GradientStop Color="#FFA726" Offset="0.25"/>
                                                    <GradientStop Color="#66BB6A" Offset="0.5"/>
                                                    <GradientStop Color="#42A5F5" Offset="1"/>
                                                </LinearGradientBrush>
                                            </Border.Background>
                                        </Border>
                                        <!-- Marker RHR -->
                                        <Border Width="38" Height="6" CornerRadius="3" Background="White"
                                                VerticalAlignment="Center" Margin="0,20,0,0"
                                                BorderBrush="#2E7D32" BorderThickness="2"/>
                                    </Grid>
                                </Border>
                            </Grid>

                            <!-- Metryki RHR -->
                            <StackPanel>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Twoja norma:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Text="50-55 bpm" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Najniższe 30 dni:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Text="48 bpm" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Najwyższe 30 dni:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Text="58 bpm" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid>
                                    <TextBlock Text="Średnia 7 dni:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Text="52 bpm" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                            </StackPanel>

                            <Border Background="#E8F5E9" CornerRadius="6" Padding="8" Margin="0,12,0,12">
                                <TextBlock Text="Stabilne, w normie ✓" FontSize="11" Foreground="#2E7D32"/>
                            </Border>

                            <Button Content="Historia RHR" Style="{StaticResource PrimaryButtonStyle}"
                                    Padding="8" FontSize="12" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>

                    <!-- KARTA 5: BILANS ENERGETYCZNY -->
                    <Border Grid.Row="2" Grid.Column="0" BorderBrush="#FFA726" BorderThickness="2"
                            CornerRadius="12" Background="White" Padding="20" Margin="0,0,8,0">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="10" ShadowDepth="3"/>
                        </Border.Effect>

                        <StackPanel>
                            <!-- Nagłówek -->
                            <Grid Margin="0,0,0,16">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="🍽️" FontSize="24" Margin="0,0,12,0"/>
                                    <TextBlock Text="Bilans Kaloryczny" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="Waga: 10%" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Główna wartość -->
                            <StackPanel Margin="0,0,0,16">
                                <TextBlock Text="-150 kcal" FontSize="36" FontWeight="Bold" Foreground="{StaticResource ErrorColor}"/>
                                <TextBlock Text="Deficyt 3 dni z rzędu" FontSize="12" Foreground="{StaticResource TextSecondaryColor}"/>
                            </StackPanel>

                            <!-- Poziomy pasek bilansu -->
                            <Border Background="#F5F5F5" Height="32" CornerRadius="16" Margin="0,0,0,16">
                                <Grid>
                                    <Border CornerRadius="16">
                                        <Border.Background>
                                            <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                                <GradientStop Color="#EF5350" Offset="0"/>
                                                <GradientStop Color="#FFA726" Offset="0.33"/>
                                                <GradientStop Color="#66BB6A" Offset="0.5"/>
                                                <GradientStop Color="#FFA726" Offset="0.67"/>
                                                <GradientStop Color="#FF7043" Offset="1"/>
                                            </LinearGradientBrush>
                                        </Border.Background>
                                    </Border>
                                    <!-- Marker bilansu -->
                                    <Border Width="16" Height="16" CornerRadius="8" Background="White"
                                            HorizontalAlignment="Left" Margin="80,0,0,0"
                                            BorderBrush="{StaticResource ErrorColor}" BorderThickness="2"/>
                                </Grid>
                            </Border>

                            <!-- Metryki bilansu -->
                            <StackPanel>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Spożyte:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Text="2,450 kcal" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="Spalone:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Text="2,600 kcal" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid Margin="0,0,0,4">
                                    <TextBlock Text="(w tym trening):" FontSize="10" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Text="650 kcal" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                                <Grid>
                                    <TextBlock Text="7-dniowy bilans:" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"/>
                                    <TextBlock Text="-820 kcal" FontSize="11" FontWeight="SemiBold"
                                               Foreground="{StaticResource ErrorColor}" HorizontalAlignment="Right"/>
                                </Grid>
                            </StackPanel>

                            <Border Background="#FFF3E0" CornerRadius="6" Padding="8" Margin="0,12,0,12"
                                    BorderBrush="{StaticResource WarningColor}" BorderThickness="1">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="⚠️" FontSize="14" Margin="0,0,8,0"/>
                                    <TextBlock Text="Przedłużający się deficyt może wpłynąć na regenerację"
                                               FontSize="11" TextWrapping="Wrap" Foreground="{StaticResource WarningColor}"/>
                                </StackPanel>
                            </Border>

                            <Button Content="Zobacz żywienie" Style="{StaticResource PrimaryButtonStyle}"
                                    Padding="8" FontSize="12" HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>

                    <!-- KARTA 6: INNE METRYKI -->
                    <Border Grid.Row="2" Grid.Column="1" BorderBrush="{StaticResource DividerColor}" BorderThickness="2"
                            CornerRadius="12" Background="White" Padding="20" Margin="8,0,0,0">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.08" BlurRadius="10" ShadowDepth="3"/>
                        </Border.Effect>

                        <StackPanel>
                            <!-- Nagłówek -->
                            <Grid Margin="0,0,0,16">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📊" FontSize="24" Margin="0,0,12,0"/>
                                    <TextBlock Text="Inne Metryki" FontSize="18" FontWeight="Bold" VerticalAlignment="Center"/>
                                </StackPanel>
                                <TextBlock Text="Waga: 5%" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"
                                           HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            </Grid>

                            <!-- Lista wskaźników -->
                            <StackPanel>
                                <Grid Margin="0,0,0,12">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="✓" FontSize="16" Foreground="{StaticResource SuccessColor}" Margin="0,0,8,0"/>
                                        <TextBlock Text="Temperatura ciała" FontSize="13"/>
                                    </StackPanel>
                                    <TextBlock Text="36.6°C" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>

                                <Grid Margin="0,0,0,12">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="✓" FontSize="16" Foreground="{StaticResource SuccessColor}" Margin="0,0,8,0"/>
                                        <TextBlock Text="SpO2" FontSize="13"/>
                                    </StackPanel>
                                    <TextBlock Text="98%" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>

                                <Grid Margin="0,0,0,12">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="✓" FontSize="16" Foreground="{StaticResource SuccessColor}" Margin="0,0,8,0"/>
                                        <TextBlock Text="Poziom stresu" FontSize="13"/>
                                    </StackPanel>
                                    <TextBlock Text="35/100" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>

                                <Grid Margin="0,0,0,12">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="⚡" FontSize="16" Foreground="{StaticResource WarningColor}" Margin="0,0,8,0"/>
                                        <TextBlock Text="Nawodnienie" FontSize="13"/>
                                    </StackPanel>
                                    <TextBlock Text="Średnie" FontSize="13" FontWeight="SemiBold"
                                               Foreground="{StaticResource WarningColor}" HorizontalAlignment="Right"/>
                                </Grid>

                                <Grid>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="✓" FontSize="16" Foreground="{StaticResource SuccessColor}" Margin="0,0,8,0"/>
                                        <TextBlock Text="Kroki wczoraj" FontSize="13"/>
                                    </StackPanel>
                                    <TextBlock Text="8,450" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>

            <!-- ==================== SEKCJA: WYKRESY TRENDÓW ==================== -->
            <StackPanel Margin="0,0,0,24">
                <Grid Margin="0,0,0,16">
                    <TextBlock Text="📉 Trendy wellness" FontSize="20" FontWeight="SemiBold" VerticalAlignment="Center"/>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <RadioButton Content="7 dni" IsChecked="True" GroupName="TrendPeriod" Margin="0,0,16,0"/>
                        <RadioButton Content="30 dni" GroupName="TrendPeriod" Margin="0,0,16,0"/>
                        <RadioButton Content="3 miesiące" GroupName="TrendPeriod"/>
                    </StackPanel>
                </Grid>

                <!-- Wykres 1: Wellness Score w czasie -->
                <Border BorderBrush="{StaticResource DividerColor}" BorderThickness="1" CornerRadius="8"
                        Height="300" Background="White" Padding="16" Margin="0,0,0,16">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8" ShadowDepth="2"/>
                    </Border.Effect>

                    <Grid>
                        <TextBlock Text="[WYKRES AREA CHART - WELLNESS SCORE W CZASIE]" FontSize="24" FontStyle="Italic"
                                   Foreground="{StaticResource TextSecondaryColor}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <!-- Legenda stref -->
                        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Right" Margin="16">
                            <Border Background="#C8E6C9" Width="80" Height="20" CornerRadius="4" Margin="0,0,0,4">
                                <TextBlock Text="70-100" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <Border Background="#FFF9C4" Width="80" Height="20" CornerRadius="4" Margin="0,0,0,4">
                                <TextBlock Text="40-70" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <Border Background="#FFCDD2" Width="80" Height="20" CornerRadius="4">
                                <TextBlock Text="0-40" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Wykres 2: Korelacja komponentów (stacked area) -->
                <Border BorderBrush="{StaticResource DividerColor}" BorderThickness="1" CornerRadius="8"
                        Height="280" Background="White" Padding="16" Margin="0,0,0,16">
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="8" ShadowDepth="2"/>
                    </Border.Effect>

                    <Grid>
                        <TextBlock Text="[STACKED AREA CHART - WKŁAD KOMPONENTÓW]" FontSize="24" FontStyle="Italic"
                                   Foreground="{StaticResource TextSecondaryColor}"
                                   HorizontalAlignment="Center" VerticalAlignment="Center"/>

                        <!-- Legenda komponentów -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="16">
                            <CheckBox IsChecked="True" Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="12" Height="12" Background="#2196F3" CornerRadius="2" Margin="0,0,4,0"/>
                                    <TextBlock Text="HRV" FontSize="10"/>
                                </StackPanel>
                            </CheckBox>
                            <CheckBox IsChecked="True" Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="12" Height="12" Background="#9C27B0" CornerRadius="2" Margin="0,0,4,0"/>
                                    <TextBlock Text="Sen" FontSize="10"/>
                                </StackPanel>
                            </CheckBox>
                            <CheckBox IsChecked="True" Margin="0,0,8,0">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="12" Height="12" Background="#4CAF50" CornerRadius="2" Margin="0,0,4,0"/>
                                    <TextBlock Text="Load" FontSize="10"/>
                                </StackPanel>
                            </CheckBox>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Statystyki pod wykresami -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="White" CornerRadius="8" Padding="16" Margin="0,0,8,0">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="6" ShadowDepth="2"/>
                        </Border.Effect>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Średni wellness" FontSize="12" Foreground="{StaticResource TextSecondaryColor}"
                                       HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="74" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,0">
                                <TextBlock Text="↗️" FontSize="12" Margin="0,0,4,0"/>
                                <TextBlock Text="+2 punkty" FontSize="11" Foreground="{StaticResource SuccessColor}"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="16" Margin="8,0">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="6" ShadowDepth="2"/>
                        </Border.Effect>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Dni w formie" FontSize="12" Foreground="{StaticResource TextSecondaryColor}"
                                       HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="18/30" FontSize="32" FontWeight="Bold" HorizontalAlignment="Center"/>
                            <Border Background="#E3F2FD" Height="8" CornerRadius="4" Margin="0,8,0,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="60*"/>
                                        <ColumnDefinition Width="40*"/>
                                    </Grid.ColumnDefinitions>
                                    <Border Grid.Column="0" Background="{StaticResource PrimaryColor}" CornerRadius="4,0,0,4"/>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="2" Background="White" CornerRadius="8" Padding="16" Margin="8,0">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="6" ShadowDepth="2"/>
                        </Border.Effect>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Najlepszy dzień" FontSize="12" Foreground="{StaticResource TextSecondaryColor}"
                                       HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="92" FontSize="32" FontWeight="Bold" Foreground="{StaticResource SuccessColor}"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="3 listopada" FontSize="11" Foreground="{StaticResource TextSecondaryColor}"
                                       HorizontalAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="3" Background="White" CornerRadius="8" Padding="16" Margin="8,0,0,0">
                        <Border.Effect>
                            <DropShadowEffect Color="#000000" Opacity="0.05" BlurRadius="6" ShadowDepth="2"/>
                        </Border.Effect>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Najgorszy dzień" FontSize="12" Foreground="{StaticResource TextSecondaryColor}"
                                       HorizontalAlignment="Center" Margin="0,0,0,8"/>
                            <TextBlock Text="41" FontSize="32" FontWeight="Bold" Foreground="{StaticResource ErrorColor}"
                                       HorizontalAlignment="Center"/>
                            <TextBlock Text="17 listopada" FontSize="10" Foreground="{StaticResource TextSecondaryColor}"
                                       TextWrapping="Wrap" TextAlignment="Center" Margin="0,4,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>

            <!-- ==================== SEKCJA: INSIGHTS ==================== -->
            <StackPanel Margin="0,0,0,24">
                <TextBlock Text="💡 Twoje wzorce wellness" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,16"/>

                <Border CornerRadius="12" Padding="24">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#E3F2FD" Offset="0"/>
                            <GradientStop Color="#BBDEFB" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#000000" Opacity="0.1" BlurRadius="12" ShadowDepth="4"/>
                    </Border.Effect>

                    <StackPanel>
                        <!-- Insight 1 -->
                        <Border Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,12" Opacity="0.95">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="📅" FontSize="20" Margin="0,0,12,0"/>
                                    <TextBlock Text="Najlepiej czujesz się w weekendy" FontSize="16" FontWeight="SemiBold"/>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,8">
                                    Twój średni wellness w soboty i niedziele: 81. W dni robocze: 72 (11% niżej).
                                    Prawdopodobny powód: lepszy sen w weekendy (+45 min średnio).
                                </TextBlock>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💡" FontSize="14" Margin="0,0,8,0"/>
                                    <TextBlock Text="Rekomendacja: Spróbuj wcześniej kłaść się w tygodniu"
                                               FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource PrimaryColor}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Insight 2 -->
                        <Border Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,12" Opacity="0.95">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="❤️" FontSize="20" Margin="0,0,12,0"/>
                                    <TextBlock Text="HRV reaguje na ciężkie treningi" FontSize="16" FontWeight="SemiBold"/>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,8">
                                    Po dniach z TSS &gt;250, Twoje poranne HRV spada średnio o 8ms.
                                    Recovery trwa zazwyczaj 2 dni.
                                </TextBlock>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💡" FontSize="14" Margin="0,0,8,0"/>
                                    <TextBlock Text="Rekomendacja: Planuj 2 dni recovery po ciężkich sesjach"
                                               FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource PrimaryColor}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Insight 3 -->
                        <Border Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,12" Opacity="0.95">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="🍽️" FontSize="20" Margin="0,0,12,0"/>
                                    <TextBlock Text="Deficyt wpływa na regenerację" FontSize="16" FontWeight="SemiBold"/>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,8">
                                    Dni z deficytem &gt;500 kcal: średni wellness 65. Dni z bilansem ±200 kcal: średni wellness 78.
                                </TextBlock>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💡" FontSize="14" Margin="0,0,8,0"/>
                                    <TextBlock Text="Rekomendacja: Unikaj dużych deficytów po ciężkich treningach"
                                               FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource PrimaryColor}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Insight 4 -->
                        <Border Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,12" Opacity="0.95">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="✓" FontSize="20" Foreground="{StaticResource SuccessColor}" Margin="0,0,12,0"/>
                                    <TextBlock Text="Regularność poprawia wellness" FontSize="16" FontWeight="SemiBold"/>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,8">
                                    W tygodniach z 4-5 treningami: średni wellness 77. W tygodniach z &lt;3 lub &gt;6 treningami: średni wellness 68.
                                </TextBlock>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💡" FontSize="14" Margin="0,0,8,0"/>
                                    <TextBlock Text="Rekomendacja: Trzymaj się 4-5 treningów/tydzień"
                                               FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource PrimaryColor}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <!-- Insight 5 - Ostrzeżenie -->
                        <Border Background="White" CornerRadius="8" Padding="16" Opacity="0.95"
                                BorderBrush="{StaticResource WarningColor}" BorderThickness="2">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                    <TextBlock Text="⚠️" FontSize="20" Margin="0,0,12,0"/>
                                    <TextBlock Text="Wykryto trend spadkowy" FontSize="16" FontWeight="SemiBold"
                                               Foreground="{StaticResource WarningColor}"/>
                                </StackPanel>
                                <TextBlock TextWrapping="Wrap" FontSize="13" Foreground="{StaticResource TextSecondaryColor}" Margin="0,0,0,8">
                                    Wellness spadł o 9 punktów w ostatnich 5 dniach.
                                    Główne czynniki: pogorszona jakość snu + rosnące ATL.
                                </TextBlock>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="💡" FontSize="14" Margin="0,0,8,0"/>
                                    <TextBlock Text="Alert: Rozważ dodatkowy dzień odpoczynku w tym tygodniu"
                                               FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource WarningColor}"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>

                        <Button Content="Zobacz wszystkie insights" Style="{StaticResource PrimaryButtonStyle}"
                                Margin="0,16,0,0" Padding="12" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>

        </StackPanel>
    </ScrollViewer>
</UserControl>
